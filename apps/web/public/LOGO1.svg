<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64" fill="none" aria-hidden="true">
  <!-- Stroke thickness = 6
       Bar length (L bottom & F top) = 18
  -->

  <!-- L -->
  <rect x="10" y="18" width="6" height="28" rx="1.2" fill="currentColor"/>
  <rect x="10" y="40" width="18" height="6" rx="1.2" fill="currentColor"/>

  <!-- Dot (match thin style) -->
  <circle cx="32" cy="31" r="3" fill="currentColor"/>

  <!-- F -->
  <rect x="40" y="18" width="6" height="28" rx="1.2" fill="currentColor"/>
  <!-- F top bar: SAME length as L bar (18) -->
  <rect x="40" y="18" width="18" height="6" rx="1.2" fill="currentColor"/>
  <!-- F mid bar: slightly shorter -->
  <rect x="40" y="29" width="14" height="6" rx="1.2" fill="currentColor"/>
</svg>
